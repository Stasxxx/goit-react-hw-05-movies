import { Routes } from "react-router-dom";

export const App = () => {
  return (
    <div>
      <Routes>
        {/* <Route path="/" element={<Home />} />
        <Route path="/movies" element={<Movies />} /> */}
      </Routes>
    </div>
  );
};
