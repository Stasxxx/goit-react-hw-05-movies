

export const MoviesFoundList = () => {
return null
    
    // return (
    //     <ul>
    //             {trendingMovies.map(({id, title,}) => (
    //             <li key={id}>
    //              <Link to={`/movies/${id}`}>{title}</Link>   
    //             </li>
    //             ))}
                
    //         </ul>
    // )
}